# ── Required ──────────────────────────────────────────────
MONGODB_URI=mongodb://localhost:27018/rise_of_the_general
JWT_SECRET=change-me-in-production

# ── Optional (defaults shown) ─────────────────────────────
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# ── AI content generation (all optional) ──────────────────
# Provide ONE of these keys to enable AI features:
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Model to use (defaults: gpt-4o-mini for OpenAI, claude-3-haiku-20240307 for Anthropic)
AI_MODEL=gpt-4o-mini

# Feature flags — set to "true" to enable each AI feature
AI_CAMPAIGNS=true
AI_NARRATIVE=true
AI_EVENTS=true
AI_OFFICERS=true