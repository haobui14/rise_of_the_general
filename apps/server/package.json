{
  "name": "@rotg/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed": "tsx --env-file .env src/scripts/seedGameData.ts",
    "migrate": "tsx src/scripts/migrateDatabase.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@rotg/shared-types": "workspace:*",
    "fastify": "^5.7.4",
    "@fastify/cors": "^11.2.0",
    "@fastify/jwt": "^9.0.0",
    "fastify-plugin": "^5.1.0",
    "mongoose": "^8.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "tsx": "^4.21.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
